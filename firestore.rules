rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY: open the schedules collection to all users
    match /schedules/{docId} {
      allow read, write: if true;
    }
  }
}
